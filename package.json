{
  "name": "starter-typescript",
  "version": "1.0.0",
  "description": "Starter typescript project for quick prototyping",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/ node_modules/ package-lock.json yarn.lock && yarn install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/index.js"
  },
  "author": "andrew.gutierrez@stardust.gg",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.14.6",
    "typescript": "^4.9.5"
  },
  "dependencies": {
  }
}
